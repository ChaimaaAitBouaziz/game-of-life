pipeline {
    agent any
    
    stages{

     stage('Checkout') {
         steps{
             checkout scm
         }
     }
     stage('Clean') {
         steps{
                 sh "mvn clean"
         }
     }
     stage('Packaging Dev') {
         when {
             branch 'develop'
         }
         steps{
             sh "mvn package -PbuildNumber=${currentBuild.number} -PgitCommit=${env.GIT_COMMIT[0..6]} "
             archiveArtifacts artifacts: 'gameoflife-web//target/*.war', fingerprint: true
         }
     }
     stage('DeployDev') {
         when {
             branch 'develop'
         }
         steps{
             sh "cd /etc/ansible"
             sh "ansible-playbook -i hosts /etc/ansible/playdep01.yml --private-key stage.pem"
         }
     }
     stage('DeployMaster') {
         when {
             branch 'master'
         }
         steps{
             sh "cd /etc/ansible"
             sh "ansible-playbook -i hosts /etc/ansible/playdep02.yml --private-key stage.pem"
         }
     }
    }
}
